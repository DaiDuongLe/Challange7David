Requirement: When user clicks on approval button, move HIS file from uploadFiles to DataDir

Algorithm:
User Clicks Select Files
User Select Files when file manager prompted and Clicks Upload
User waits for file to Load
User Clicks Upload
Files are uploaded to uploadFiles
User Selects Name with It's UUID
File selected for Approval
User Clicks Approve
User's file from uploadFiles is moved to DataDir

<table id="table" class="display">
  <thead>
  <tr>
    <th>ID</th>
    <th>First Name</th>
    <th>Last Name</th>
    <th>First Name</th>
    <th>Time Stamp</th>
  </tr>
  </thead>
  <tbody>

    <% for(var i = 0; i < data.length; i++) { %>

    <td><%=data[i].ID%></td>

    <%} %>

<%=console.log(i)%>

  <tr>
    <td><%=data[0].LastName%></td>

  </tr>
  </tbody>
</table>


